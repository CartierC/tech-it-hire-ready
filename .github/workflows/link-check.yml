name: Link Check

on:
  push:
  pull_request:

jobs:
  link-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run PowerShell link check
        shell: pwsh
        run: |
          ./scripts/verify-links.ps1
